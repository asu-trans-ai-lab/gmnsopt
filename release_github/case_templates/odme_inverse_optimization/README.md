# Template — ODME & Inverse Optimization (`odme_inverse_optimization`)

Maturity: **runnable**. Formulation classes: LP, QP, NLP, stochastic, inverse.
Subtypes: od_matrix_estimation, path_flow_correction, sensor_residual_min, demand_tensor_calibration.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: odme.
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
