# Template — Routing & Accessibility (`routing_accessibility`)

Maturity: **runnable**. Formulation classes: LP, network_flow.
Subtypes: shortest_path, multimodal_path, accessibility, equity_aware_access.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: shortest_path, accessibility, min_cost_flow.
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
