# Template — Signal & Queue Spillback Control (`signal_queue_control`)

Maturity: **runnable**. Formulation classes: MIP, dynamic, stochastic, RL_ready.
Subtypes: signal_timing, green_split_offset_phase, queue_spillback, corridor_mpc_milp_rl.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: signal_timing.
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
