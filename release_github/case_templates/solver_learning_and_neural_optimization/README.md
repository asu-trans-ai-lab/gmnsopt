# Template — Solver Learning & Neural Optimization (`solver_learning_and_neural_optimization`)

Maturity: **scaffold**. Formulation classes: ML, RL, surrogate.
Subtypes: learning_to_warm_start, learning_to_branch, graph_neural_features, neural_surrogate, rl_policy_interface, train_val_test_city_split.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: ml_features.
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
