# Template — Transit Frequency & Accessibility (`transit_frequency_accessibility`)

Maturity: **planned**. Formulation classes: MIP, LP, stochastic.
Subtypes: bus_frequency, fleet_allocation, first_last_mile, transfer_coordination, equity_service.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: (scaffold/planned — see docs/benchmark_ladder.md).
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
