# Template — UAM Air–Ground Coordination (`uam_air_ground_coordination`)

Maturity: **scaffold**. Formulation classes: MIP, stochastic, dynamic, RL.
Subtypes: vertiport_capacity, dispatch_repositioning, weather_uncertainty, battery_charging, ground_access_integration.

Copy this folder to `cases/<id>/`, drop GMNS files into `input/`, edit `problem.yml`, then `gmns-opt run`.
Runnable model templates for this family: multimodal_skeleton.
See `formulation.md` for the math and `../../docs/application_taxonomy.md` for context.
