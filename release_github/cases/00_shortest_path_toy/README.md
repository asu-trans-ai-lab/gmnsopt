# GMNS-SP-101 — shortest path & accessibility (toy)
Least-cost path on a 5-node GMNS diamond from zone 1 to zone 2. Entry-level: teaches the LP/network view of
routing and validates the GMNS reader. Run: `gmns-opt run .` → `output/solution.csv` (path 1→2→4→5, 6 min).
