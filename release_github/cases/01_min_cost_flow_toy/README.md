# min-cost flow (toy)
Ship 600 units from zone 1 (source) to zone 2 (sink) at minimum generalized cost on the same GMNS diamond,
respecting link capacity. Run: `gmns-opt run .` → per-link flow in `output/solution.csv` (objective 3600).
